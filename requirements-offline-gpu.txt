# Offline-only dependencies (GPU/text models).
#
# These packages are NOT needed at Stage-3 replay time and should not be baked
# into the slim CPU-only `requirements.txt`. Install this file only when running
# offline pipelines like `offline/02_text_scores.py` with dense/reranker models.
#
# NOTE: Pick the appropriate torch wheel for your CUDA version. If you're on
# Windows, you may prefer installing torch via the official index URL rather
# than pinning it here. We keep the rest pinned for determinism.

transformers==4.56.2
accelerate==1.8.1
safetensors==0.4.5
tokenizers==0.22.1
sentencepiece==0.2.0
tqdm==4.66.6

